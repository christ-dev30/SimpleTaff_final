const jsdom = require('jsdom'); const { JSDOM } = jsdom; const fs = require('fs'); const html = fs.readFileSync('src/main/resources/static/admin-entreprise/index.html', 'utf8'); const virtualConsole = new jsdom.VirtualConsole(); virtualConsole.sendTo(console); virtualConsole.on('jsdomError', e => console.error('JSDOM Error:', e)); const dom = new JSDOM(html, { runScripts: 'dangerously', virtualConsole });

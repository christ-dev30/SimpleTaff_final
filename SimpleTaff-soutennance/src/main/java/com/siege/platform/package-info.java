@FilterDef(
    name = "tenantFilter",
    parameters = @ParamDef(name = "entrepriseId", type = java.util.UUID.class)
)
package com.siege.platform;

import org.hibernate.annotations.FilterDef;
import org.hibernate.annotations.ParamDef;
